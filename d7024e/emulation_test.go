package d7024e

import (
	/*"testing"
	"log"
	"time"
	"math/rand" */
)

/* Test case 3001: Simulate a network of 100 peers performing tasks. */
/*func test_3001(t *testing.T) {

	//create 100 nodes
	port := 8000;
	nodes := make([]*d.Node, 0)
	for i := 0; i < 100; i++ {
		newNode := d.NewNode("", "localhost:" + strconv.Itoa(port))
		go newNode.NodeUp()

		//the node needs to know of at least one no
		nodes = append(nodes, newNode)
		port ++;
	}


	log.Println("Nodes are up") 
}*/